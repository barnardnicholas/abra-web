import { Scenario, soundTypes } from '../../types/Scenario';

const none: Scenario = {
    id: 1,
    name: 'None',
    slug: 'none',
    description: 'No scenario loaded',
    sounds: {},
};

export default none;
